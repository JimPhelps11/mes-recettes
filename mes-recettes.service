[Unit]
Description=Mes Recettes - Web App
After=network.target

[Service]
Type=simple
User=hermes
WorkingDirectory=/opt/data/home/recettes
ExecStart=/opt/data/home/recettes/start.sh
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
