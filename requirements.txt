Flask==3.0.3
Flask-CORS==4.0.0
python-dotenv==1.0.0
openpyxl==3.1.2

# Pour le voice TTS local (si tu utilises gTTS précédemment)
gTTS==2.3.2

# Pour les requêtes réseau (si tu appelles des APIs externes)
requests==2.31.0
