/**
 * PlateSimulator – module placeholder pour évite les erreurs d'import
 */
class PlateSimulator {
  static init() { /* no-op */ }
}

if (typeof document !== 'undefined') {
  document.addEventListener('DOMContentLoaded', PlateSimulator.init)
}

